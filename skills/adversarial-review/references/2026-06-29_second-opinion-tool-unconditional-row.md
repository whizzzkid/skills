---
class: principle
skill: wk-adversarial-review
date: 2026-06-29
---

**Rule** — Detecting and invoking a configured second-opinion / automated-review
tool must be an unconditional mechanical sweep row (2.0), not a preamble bullet.
Probe presence each run (`command -v <tool>` / env flag); present-but-not-invoked
is a finding.

**Why** — A re-violation: the prior fold expressed this as two prose bullets
above the sweep table, and the invocation was still skipped (user had to ask
"are you running the local review?"). Ambient prose and memory rules lose to
context pressure; only a row governed by "run every sweep unconditionally"
guarantees execution. Terminal escalation rung — restructure so the rule cannot
be skipped.

**Where** — Step 2 mechanical sweep catalog, row 2.0. Replaced the two preamble
bullets (net de-bloat). The binding tool-specific invocation rule lives in the
user's private global instructions, not the public skill.
