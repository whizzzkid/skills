---
class: principle
---

**Rule** — Step 3's drift-audit rule escalated one notch (baseline prose →
`**Important:**`) after a run skipped both the Step 3 description drift audit and the
Step 8 post-push sync.

**Why** — The rule already existed and was re-violated with no same-session evidence it
fired correctly, so per the re-violation-escalation ladder it moves up exactly one notch
rather than staying `already-covered`.

**Where** — `skills/pr-resolve/SKILL.md`, Step 3 "agent-observed drift is first-class
feedback"; Step 8 sync rule is already a HARD RULE (unchanged).
