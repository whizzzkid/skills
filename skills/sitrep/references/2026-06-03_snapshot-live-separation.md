---
class: principle
---

- **Rule:** Snapshot holds historical record only (`[x]` + notes + metrics); live.md owns ALL pending `[ ]` work. Every item has a link or is omitted; nested checkboxes for sub-tasks; sort by urgency (🔴/🟡/🟢); due-dates as `**📅 YYYY-MM-DD**`; no separate `.last_working_day` file (date lives in frontmatter).
- **Why:** The first run mixed historical achievements with pending items, left live.md nearly empty, and lacked consistent date/urgency/link formatting in the SilverBullet rendering context.
- **Where:** "HARD RULE — SilverBullet markdown formatting"; end Stage 4 (snapshot historical-only) + Stage 5 (live.md owns pending); Step 0 marker file removed.
