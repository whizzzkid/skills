---
class: principle
skill: wk-sharpen
date: 2026-07-25
severity: high
---

**Rule** — Every load-bearing zero in this skill takes the `command` prefix, including the
Step 3 prohibited-subject gate. The mandate previously sat only on the Step 5 staged-path
hand-roll bullet, so the Step 3 gate — whose whole purpose is a zero result — instructed a
bare `grep`.

**Why** — A rule stated at one call site does not govern the others. The prohibited-subject
gate is the earliest scan in the run and the one whose false-clean is most costly: it
decides whether a lesson may land in the public repo at all. Scoping the `command` mandate
to a later, narrower scan left the highest-stakes zero unprotected while reading, at a
glance, as though the skill already required it.

**Classification** — `partial`, not `already-covered`. Matching at rule level rather than
topic level: the skill's existing text covers *invoke `command grep`* (Step 5 staged-path
bullet), *a missing path is not a clean result* (Step 2), and *run the owning hook rather
than hand-rolling* (Step 5 CRITICAL). It did **not** cover the Step 3 gate's own
invocation form. The general principle is folded into the shell-workstyle skill that owns
the silent-failure trap family; this file records the site fix.

**Escalation** — None. The general rule was newly added this pass rather than repeated, and
the site that failed had no rule to violate.

**Where** — Step 3, prohibited-subject gate.
