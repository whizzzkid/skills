---
class: principle
---

**Rule** — At distill time, grep the learning's core subject term across the whole
`skills/` tree, not only the skill the learning names. Fold the principle into the
API-mechanics home skill AND correct every over-general instance found elsewhere in the
same pass.

**Why** — A learning filed under one skill can describe a defect whose text lives verbatim
in another. Scoping the read to the filed skill finds nothing to fix and ships the real
defect uncorrected.

**Where** — `skills/sharpen/SKILL.md` Step 2 "Read the Full Skill". Reclaimed bytes by
deleting the redundant "Anti-Patterns to Avoid" table (duplicated the Core Rule section +
overfit-scan categories).
