#!/usr/bin/env bash
# Stop hook script: suggests running a retro at session end.
# Install by adding a Stop hook to your settings.json that calls this script.

echo "Session ending. Run /wk:retro to capture learnings from this session."
