## Session-12

### What worked
- Proc-tuple step pattern `[label, cmd, post_proc]` — decouples post-step side effects from label strings, eliminating rename fragility at zero cost.
- Adversarial subagent caught a seed/write-back gate asymmetry before push — the exact class of bug the seed was added to prevent.
- Union-merge conflict resolution (keep both sides) handled spec divergence cleanly without re-running tests.

### What could've been better
- wk-adversarial-review: Ruby lambda syntax `-> { rescue }` inside an array literal is a parse error — needs `-> do ... rescue ... end`. Add this to the Ruby sweep catalog.
- wk-adversarial-review: `Style/AsciiComments` cop fires on em-dash and curly quotes in comments. Sweep 2.39 exists but doesn't mention the `-> do...end` multi-line lambda form; add the two patterns together.
- wk-pr-resolve: bot inline-comment REST databaseIds become 404 after code churn (outdated threads have `line: None`). Fallback is GraphQL `resolveReviewThread` + a summary conversation comment. This pattern should be in the skill's thread-resolution section.
