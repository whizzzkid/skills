## Session-1

### What worked
- Parallel multi-agent data gathering produces a comprehensive, well-sourced brief in one pass.
- Real-time wk-learn capture at each correction yields precise learnings; the retro scan then confirms completeness.

### What could've been better
- wk-goodmorning: enforce the interview-prep scan as a hard gate before data-gathering agents, even when the interview is known from carry-over.
- wk-goodmorning: every standup bullet carries exactly one external link; group related artifacts as a parent bullet with nested children.
- wk-goodmorning: standup clipboard copy must use `ClipboardItem` (`text/html` + `text/plain` fallback), not bare URLs or Slack mrkdwn.
- wk-goodmorning: standup HTML must use real nested `<ul><li>`, never flat headings.
- wk-goodmorning: label PR links `{repo}#{number}`, never bare `#NNN`.
- wk-goodmorning: never include interview PII or personal/HR items — the standup is a public team artifact.
- wk-slack: own the canonical standup structure, formatting, and clipboard behavior; wk-goodmorning and wk-goodevening delegate to it rather than re-implementing.
