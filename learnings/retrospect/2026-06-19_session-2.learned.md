## Session-2

### What could've been better
- **wk-pr:** Self-review parallel posting rule from a prior learning (`2026-05-26_self-review-before-ci-poll.learned.md`) had been distilled but never re-incorporated into the skill's numbered Step 3 gates; agent defaulted to intuitive serial ordering (CI green → then review). Distilled learning (now in `2026-06-18_self-review-parallel-repeated-error.md`) must land as an explicit blocker in wk-pr Step 3 instructions.
- **wk-pr:** Final adversarial gate (Step 5) re-runs unconditionally before `gh pr ready`, but should carve out an exception for the common case where only commits since last clear verdict are mechanical fixes directly addressing that verdict's blockers. Scoped exception captured in `2026-06-18_skip-redundant-adversarial-gate.md`.
