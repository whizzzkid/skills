## Session-N

### What worked
- A pre-push adversarial-review "question" about response honesty (returning a received-but-never-persisted id) was treated as a real defect and fixed at source, not argued down — cheap to fix before review, expensive after.

### What could've been better
- wk-adversarial-review: a migration that renames or drops a DB index/constraint must trigger a repo-wide grep for the OLD name (schema/structure specs pin index names as string literals), since those specs sit outside the migration's own diff surface and a diff-scoped sweep misses them — this reached CI as a red RSpec run.
