## Session-2

### What worked

- Treat a non-fast-forward push as concurrent work: fetch, inspect, and
  preserve the remote history before retrying.

### What could've been better

- [`wk-pr-update`](../../skills/pr-update/README.md): Document the
  remote-base-merge-after-local-commit path so agents preserve both histories,
  then revalidate before a normal push.
