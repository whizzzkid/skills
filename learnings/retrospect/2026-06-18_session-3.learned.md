## Session-3

### What worked
- Adversarial review caught a bash fake branch missing a terminal `exit 0`, which would have caused tests to pass by exercising the wrong path
- `wk-learn scan` dedup check prevented rewriting learning files already captured earlier in the session

### What could've been better
- wk-workflow: Confirm format strings from feature-request snippets before encoding across multiple files; two correction cycles resulted from treating an illustrative example as the production spec
- wk-workflow: Before encoding a constant in a new code path, grep sibling paths for analogous values; the stable code path already had dynamic platform detection one scroll away
