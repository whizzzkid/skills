## Session-N

### What worked
- Reconsidering a prior-round dismissal when the bot re-fires with a concrete repro (hypothetical → named failing case) is the right call, not a flip-flop — treat new evidence as grounds to upgrade dismiss→fix.
- Collapsing two findings that share one dataflow (comparator perf + missing index guard) into a single decorate-sort-undecorate restructure is one triage unit, not two commits.

### What could've been better
- pr-resolve: a merge-ready/"land this" directive in the invocation vs Hard Rule 1's per-push confirmation is ambiguous — define which phrases constitute standing push authorization.
