## Session-3

### What worked
- Verify post-push feedback across every review surface and require local, remote, and PR head identity before completion.

### What could've been better
- [wk-workstyle-shell]: Preserve captured structured payloads with `printf '%s'` or a direct pipe instead of `echo`.
- [wk-gh]: Probe available `gh pr checks` fields and derive required contexts from repository policy when needed.
