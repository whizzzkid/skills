## Session-11

### What worked
- Adversarial review self-caught three real defects (dead framework target, an untracked timer, an unthrottled global pointer listener) before publish — running the mechanical sweep + fresh subagent on a UI-only diff paid off.
- When the local DB was unusable, manual browser-driven validation plus the authoritative CI suite substituted cleanly for blocked local tests — never claimed local specs passed; stated the block and its cause.

### What could've been better
- workflow: default brand/decorative animation to motion-only channels and hold color constant unless color animation is explicitly requested; validate theme-aware UI in both light and dark mode before declaring done.
