## Session-4

### What worked
- Confirm a watched CI run with a fresh full-rollup query and remote-head comparison before declaring the pull
  request green.
- Classify a failure as flaky only after matching its exact assertion against independent passing local,
  sibling-branch, and rerun evidence.

### What could've been better
- wk-gh: Use standalone `jq` whenever a GitHub CLI JSON projection needs bound variables because `gh --jq` accepts
  only an expression.
