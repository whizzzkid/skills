## Session-1

### What worked
- Conflict resolution via `git merge` (not patch-replay) preserves atomic history and matches PR ready-for-review state.
- Adversarial-review gate caught CSS specificity regressions that request specs miss (specificity defeat, dead selectors).
- Pending self-review route: REST `/replies` → 422 (expected) → fall back to top-level issue comment is smooth and well-documented in wk-gh.

### What could've been better
- wk-pr-merge: GraphQL call for stacked-child retargeting failed transiently; the REST fallback succeeded. Pattern for handle-and-continue on transient GraphQL failures in mid-flow operations could be clearer (Step 6 pre-merge retargeting).
