## Session-2

### What worked
- Adversarial review subagent caught a real regression (Missing Testing accidentally demoted from minor to info) that the primary implementation missed — the two-pass review pattern proved its value on a tiny diff.
- Severity reclassification was clean and complete: all four original tiers mapped correctly with no dropped conditions after the adversarial catch.

### What could've been better
- wk-jira: Jira key in the opening prompt must trigger Stage 0+1+2 before wk-workflow Phase 1 — the ticket was not claimed or moved to In Progress at session start.
- wk-pr-merge: Past-tense merge signals ("the PR is merged") must invoke wk-pr-merge before any other action — the agent executed the Jira transition inline, skipping retro and worktree cleanup.
