## Session-1

### What worked
- Adversarial subagent caught the tab lifecycle guard regression before merge — the original guard was already correct for the new domain extraction
- Freezing shipped migrations rather than editing them preserves install-base consistency

### What could've been better
- [workflow]: Before changing a guard/filter condition, verify whether the upstream function change already makes the existing guard correct — avoid unnecessary edits that introduce regressions
- [workflow]: When a function returns non-null for new inputs, existing null-checks naturally cover those inputs — don't replace a working guard with a broader one
