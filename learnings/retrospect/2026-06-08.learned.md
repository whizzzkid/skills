## Session-1

### What worked
- `<span onclick>` as a custom checkbox pattern bypasses the editor's form-element disabling
- `window.client.space.readPage/writePage` is the correct file I/O API — handles all sync layers
- CSS `:has()` for state-based styling without JavaScript is clean and persistent
- No-blank-line HTML blocks keep content inside the widget and render inline markdown (bold, links)
- `String.fromCharCode(34)` solves the double-quote-in-attribute-value problem reliably
- Playwright browser automation was effective for iterative DOM inspection and debugging

### What could've been better
- wk-silverbullet: `<input>` disabled by editor was not known upfront — 2 iterations wasted on form elements
- wk-silverbullet: service worker intercepts fetch was not known — 3 iterations wasted on HTTP API guessing
- wk-silverbullet: CommonMark blank-line HTML block rule was not known — 1 iteration lost to broken column layout
- wk-silverbullet: `>` in arrow functions inside attribute values truncates — caught only after DOM inspection
- wk-sitrep: original skill spec mandated markdown table with ⬜ glyphs; interactive checkboxes were not an original design goal but became a requirement — skill should note the div-based recipe as the preferred layout

Routed to wk-learn: silverbullet (11 files)
## Session-2

### What worked
- Handoff doc (RUN_LOCALLY.md) read and all changes applied correctly in one commit, then deleted in same commit — no separate cleanup commit needed.
- Adversarial review correctly cleared a pure path-rename diff quickly with zero findings.
- CI failure auto-investigated without user prompting; pre-existing rubocop offense identified and fixed within the same session.

### What could've been better
- buildkite: `bk build view -b` takes a branch name not a build number; piping output directly to `jq` fails when the build is not found (returns `null\n`). Save to file first or use `--json` without `-b` to get latest build safely.
## Session-3

### What worked
- Adversarial subagent caught three concrete blockers (unconditional dry-run injection, non-existent MCP write tool, silent-empty-string SHA fallback) that mechanical sweeps alone would have missed.
- Pre-existing CI failures were correctly identified as unrelated to the diff and fixed with a targeted atomic commit.

### What could've been better
- wk-workflow: When adding a new "command" to an existing skill's surface area, probe whether the capability belongs as a routing mode on the existing skill before scaffolding a new one — `/tool action` implies a subcommand, not a new tool entry.
