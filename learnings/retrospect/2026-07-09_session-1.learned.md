## Session-1

### What worked
- Adversarial-review mutation testing caught a fake-green test whose guard-removal fell through to the method's own rescue path — always mutate the guard and confirm the test actually fails before trusting it.
- Extracting duplicated gating-count logic into a single module method and routing all callers through it removed drift a bot had flagged in multiple call sites.

### What could've been better
- pr-resolve: a bot finding contradicting an earlier-accepted fix triggered a full plan-and-ask pause when the dismissal rationale was already evidence-backed; act directly on confident dispositions instead. (routed to wk-learn)
