## Session-N

### What worked
- Base detection catching a stale local default-branch ref (diff showed a huge phantom delta) and pivoting to the remote base kept the PR scope honest.
- Surfacing a discovered latent bug as an A/B fold-in-or-track decision before expanding PR scope.

### What could've been better
- wk-pr: scope measurement must diff against `origin/<base>`, not the local base ref — a stale local default branch makes `git diff base...HEAD` report merged files as phantom additions and inflates LOC.
