## Session-2

### What worked
- Refute an externally-sourced Major with code evidence (trace the actual guard) before accepting it — a plausible finding can still be a false positive.
- Before changing a `return nil` to a real value, prove behavior-preservation at every downstream consumer (reviewer-assignment gate, metrics, approved flag) so the change is provably additive.

### What could've been better
- workflow: a "don't run {tool} locally" ban plausibly covers that tool's own repo test suite — prefer the narrow spec + CI for the full suite, or confirm scope before a heavy local run.
