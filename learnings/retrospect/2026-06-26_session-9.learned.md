## Session-1

### What worked
- All-Minor bulk-dismiss gate eliminated per-item consultation overhead for 4 findings in a single round — the "prior-round dismissal same class" recognition worked as intended
- Fake curl method routing (GET/PUT/POST via flag detection) gave clean isolation for pre-flight, upload, and metadata test paths without URL coupling
- Incremental commit discipline (one commit per triage unit) kept the adversarial-review scope narrow — each re-review cleared cleanly

### What could've been better
- [pr-resolve]: bot finding about swallowed errors in a CI publish script classified as judgment-required instead of obvious-fix; fail-open in any artifact-publishing context has no valid skip rationale and should default to obvious-fix
