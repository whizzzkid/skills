## Session-5

### What worked
- Caught the App-ID-vs-installation-ID mismatch in the request before writing code, and surfaced the secret-provisioning dependency the user had not mentioned.
- Self-heal on the stale-bundle pre-commit hook failure (`bundle install` then retry) unblocked the commit without bypassing the hook.

### What could've been better
- wk-workflow: in a linked worktree, an absolute edit path resolved into the main checkout instead of the worktree copy — verify edit targets sit under the active worktree root before editing/staging.
