## Session-1

### What worked
- Minimal targeted fix: updated test to use `manifestBase.version` dynamically instead of hardcoded date
- All CI checks pass after the fix
- Properly followed user correction to create new branch from default branch

### What could've been better
- workflow: should have invoked wk-workflow at session start instead of jumping straight to implementation
- plan: should have created explicit plan with wk-plan before any code changes
- commit: should have used wk-commit for the commit instead of raw git commit