## Session-2

### What worked
- Adversarial review caught a `soft_fail` directive copied from a sibling template without contract verification — removed before push, preventing silent failure swallowing on publish errors.
- Fake binary test harness isolated the publish shell script end-to-end without real external access, exercising all happy/sad paths cleanly.
- Playground validation resolved an adversarial subagent's disputed `curl` runtime-behavior claim definitively rather than acting on an unverified blocker.

### What could've been better
- wk-adversarial-review: runtime-behavior findings from the adversarial subagent were rated `blocker` before playground validation; the skill needs an explicit rule that tool-behavior claims default to `question` until reproduced.
- wk-adversarial-review: Sweep 2.2 does not prompt a contract-transfer check when directives are copied from a sibling template; the exit-code contract should be verified, not assumed.
- wk-workflow: implementation began before the skill was invoked; chat-based planning is not a substitute for the mandatory invocation gate.
