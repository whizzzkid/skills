## Session-2

### What worked
- Dogfooded automated review findings triaged in rounds: fixed genuine correctness/duplication issues, explicitly declined a "Major" suggestion that would make code strictly worse (crash on malformed input vs. safe false), and stopped iterating once findings were advisory nits on green CI.
- Historical-review semantics caught: an approval-gate must collapse to each user's latest stance-changing state, since the reviews API returns every historical review each retaining its original state.

### What could've been better
- wk-pr: open the PR in the browser (`gh pr view --web`) as the first atomic action bound to `gh pr create`, not a skippable prose bullet — the user had to ask why it wasn't opened.
