## Session-2

### What worked
- Tracing an artifact end-to-end (does any code/CI/prompt open its content?) before trusting its self-description as a live orchestrator settled a long ambiguity that prose-level reasoning could not.
- Choosing structural deletion of a dead file over continued prose fixes ended a bot re-review loop that wording passes would never have converged.

### What could've been better
- pr-resolve: when a bot re-fires on prose in one non-code file, check whether anything consumes the file before iterating its wording — a dead artifact should be deleted, not reworded.
