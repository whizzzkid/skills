## Session-2

### What worked
- When a CLI's `--json` field errors, cross-verify the same state from a second independent source (provider API + CI vendor CLI) rather than blocking on the tool error.
- Route a blocked review-reply (pending self-review returns 422) around via the GraphQL thread-resolve path plus a single conversation-surface comment.

### What could've been better
- pr-merge: Step 2's `gh pr checks --json name,state,required` hard-codes a version-dependent field and errors with no fallback; degrade to check-runs API + CI-vendor query on an unknown-field error.
