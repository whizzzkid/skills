## Session-1

### What worked
- Extracted a shared helper (`cloudsmith_cdn_url`) to reduce nesting depth when RuboCop flagged 4-level block nesting — good reflexive refactor that improved both lint compliance and readability.
- Used a file-based call counter in the bash fake binary to simulate polling across subprocesses — correct approach for stateful bash fakes without shared memory.
- Added a `<TOOL>_BETA_POLL_INTERVAL` env override early, making all specs fast without real sleeps.

### What could've been better
- wk-workflow: Treated a version-string example in the feature request as the normative spec without verifying the exact format, requiring two correction cycles.
- wk-workflow: Hardcoded a platform/arch string in the beta path despite the adjacent stable path already computing it dynamically — failed to cross-check sibling code paths before hardcoding.
