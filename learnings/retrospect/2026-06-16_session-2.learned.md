## Session-1

### What worked
- Adversarial review gate caught a RuboCop parse error (`when 50..` → `when (50..)`) before push — the gate-before-push HARD RULE prevented a broken CI build.
- Reading CI pipeline template files (not just Ruby source) to verify cross-step artifact persistence resolved a `question`-severity adversarial finding without an unnecessary fix.

### What could've been better
- wk-gh: `in_reply_to` for inline PR review comment replies requires the numeric REST `id`, not a GraphQL node ID (`PRRC_...`); passing the wrong ID type returns 404.
