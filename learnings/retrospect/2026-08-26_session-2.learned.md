## Session-2

### What worked
- Gate completion on exact-head provider jobs and use full logs to distinguish code failures from infrastructure termination.

### What could've been better
- [wk-buildkite]: Document the UUID-only retry syntax and the signed-empty-commit fallback for query-only credentials after a verified infrastructure failure.
