## Session-3

### What worked
- To verify dedup/no-repost, fetch all bot findings across every review run and group by path|line|title, then diff the newest run's keys against the union of prior keys -- a repeat key is a dedup miss, a novel key is genuinely new.
- When consolidating a duplicate-render spec, confirm the surviving spec already covers each assertion and fold only the genuinely-unique one (here a timeout) before deleting the redundant file.

### What could've been better
- pr-resolve: a blanket "yes" to a batched push+reply+resolve plan is re-denied per external-write class by the auto-mode classifier; request per-action confirmation up front or narrate the sequential re-asks.
