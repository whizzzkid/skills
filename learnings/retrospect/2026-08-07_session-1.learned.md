## What worked

- Verified generated diagnoses against the pinned framework source and an exact database transition before choosing a fix.
- Kept repository credentials in the trusted host job and transferred only the required artifact into the test container.
- Used native database type assertions, large-value round trips, and a prior-schema upgrade rehearsal as complementary checks.

## What could've been better

- `[wk-pr-resolve]`: Treat code changes, green CI, reactions, and thread resolution as one atomic lifecycle before reporting review feedback handled.
- `[wk-workflow]`: Activate every relevant skill before the first task action so process recovery is unnecessary later.
