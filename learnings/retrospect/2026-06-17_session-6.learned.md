## Session-1

### What worked
- Reading all conflicting files in parallel before resolving any — one cognitive pass, zero back-and-forth.
- `go vet` as the post-resolution compilation check; recognising the embed error as benign from prior context.
- Detecting PR description drift (base PR status stale) and fixing it in the same push cycle.

### What could've been better
- pr-resolve: `git add` on resolved conflict files failed with exit 128 because the agent's cwd was a subdirectory, not the repo root; the skill should note that staging must be done from the repo root (via `git -C <root>` or `cd` first).
