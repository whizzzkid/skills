## Session-3

### What worked
- Adversarial review caught a path traversal security bug in `AllowedDirsForCheck` before it shipped; two-layer defense (load-time resolver + runtime `filepath.Rel` clamp) was the right call.
- All tests passed CI on first try after the security fixes were applied.

### What could've been better
- adversarial-review: when a new struct field is added alongside existing fields of the same type, check whether the new field follows the same sanitization/normalization pattern (e.g., `resolveCheckScope` was missing while `resolveCheckModel` already existed as the established pattern).
