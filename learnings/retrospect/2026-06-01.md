
## Session-1

### What worked
- Adversarial subagent correctly caught the prepend-before-empty-check regression before any code shipped to CI — the skill earned its cost.
- Mutation-style reasoning (trace what happens when all findings are in-diff + summary_line present) surfaced a high-severity blocker that unit tests missed.

### What could've been better
- wk-pr: stacked on an unmerged draft base without checking whether the parent was still a draft; user redirected to main.
- adversarial-review: the prepend-before-empty-check interaction was missed during implementation and only caught in pre-flight; the reasoning should happen at the seeding site.

## Session-2

### What worked
- Mutation test on the new assertion confirmed it was non-vacuous before push, catching a potential fake-pass before it shipped.
- Adversarial subagent findings were all traced to relocated pre-existing lines; dismissing them via merge-base grep was faster than re-fixing pre-existing issues.
- Cherry-pick recovery from an accidental rebase-over-pushed-history avoided a force-push without losing the permission-fix commit.

### What could've been better
- pr-resolve: after an autosquash rebase that rewrites pushed history, detect the diverged remote before proceeding — prefer cherry-pick or a fixup commit on top instead of rebase.
