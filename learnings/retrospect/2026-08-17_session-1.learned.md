## Session-1

### What worked
- Datadog-first investigation (monitor definition, APM traces, metrics queries) correctly distinguished the real root cause from a plausible red herring in recent git history — direct evidence over narrative inference

### What could've been better
- wk-pr: enabled auto-merge fast path without first dispatching adversarial review — the fast-path clause requires a clear adversarial-review verdict as a co-precondition, not just a small diff
