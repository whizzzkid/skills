## Session-2

### What worked
- Backward-compatible marker evolution via a stable *prefix* constant (not a literal-string swap) let every existing `include?` identity matcher keep matching both new and legacy bodies — the right pattern when evolving an embedded identity/signal string.
- Closing a coverage gap proactively (pinning dedup tests against the NEW emitted format, not just the legacy literal) before the adversarial subagent ran kept the review clean in one pass.

### What could've been better
- pr: a prod-facing behavior change reached `gh pr create` without a rollout/rollback note and was flagged reactively by a description-check bot — include the rollout section at body-composition time for prod-facing diffs, not only at review-sweep time.
