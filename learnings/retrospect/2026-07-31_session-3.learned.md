## Session-3

### What worked
- Gate stacked merges on live pull-request heads and remote topology, while treating local stack metadata as
  membership information only.
- Resolve bot feedback with focused commits, primary-source dismissals, final-head verification, and an atomic
  stack merge.

### What could've been better
- pr-merge: Detect a queued replacement workflow before treating canceled checks from a superseded same-head run
  as the final CI result.
