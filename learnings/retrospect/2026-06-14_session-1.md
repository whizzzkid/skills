## Session-1

### What worked
- Preserve workflow rules while debloating by delegating Phase 1 to `wk-plan` and invoking focused skills for later phases.
- Treat hook failures as mechanical guard feedback, then fix the authored artifact before retrying.

### What could've been better
- `wk-workflow`: encode portable `$HOME/...` placeholders for user-land configuration paths instead of bare home paths.
- `wk-learn`: validate learning filename suffix before staging a new learning file.
