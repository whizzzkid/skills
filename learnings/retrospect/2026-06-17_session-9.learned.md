## Session-1

### What worked
- Running the full test suite in background while composing the adversarial review saved round-trip time; tests were ready when review completed.
- Committing per triage unit (module-boundaries fix separate from test quality fix) kept each commit's scope clean.

### What could've been better
- adversarial-review: When a docker env array intentionally omits a variable to block a plugin-injected wrong value, the spec exercising that fallback is the only signal — the skill should check for a spec deleting the var before flagging omission as a blocker.
