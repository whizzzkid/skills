## Session-1

### What worked
- Combining multiple dependency-bump PRs: apply the version pins, run one conservative `bundle update <gems>`, then verify the lockfile delta matches only the stated targets plus explained transitive additions.
- Verifying app boot after a framework minor bump (not just tests) caught a removed-require breaking change that a spec-less suite would have missed.

### What could've been better
- pr: treated `Closes #N` as able to auto-close PRs; it only closes issues — clarify the real close-on-merge mechanism when the target is a PR.
