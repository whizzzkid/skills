## Session-1

### What worked
- Tri-state Symbol/case for auth gate disposition avoided the `performed?` test-double NoMethodError that a two-method boolean split hit.
- `git stash` diagnostic to confirm a spec failure was pre-existing flake, not a regression from current change.

### What could've been better
- wk-pr-resolve: after a shared-guard refactor, running only targeted spec files instead of the full relevant spec directory (spec/requests/, spec/controllers/) let a semantic-inversion regression reach CI; enforce full-suite verification at every refactor commit boundary.
- wk-pr-merge: `gh pr merge` and `gh api .../merge` both fail when GitHub server-side flags a PR as stack-member (independent of local `gh stack view`); skill needs a documented fallback path when the async REST endpoint is classifier-blocked (user-runs-manual + resume from already-MERGED state).
