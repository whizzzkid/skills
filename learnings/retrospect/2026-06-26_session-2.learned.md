## Session-1

### What worked
- Tight adversarial-review loop with a parallel review-scanner binary finding actionable findings (trigger phrases, guards, doc sync) on first run
- Fast iteration on 4 surgical edits + adversarial verify + commit + push cycle — no rework loop needed after fixes landed
- Thread resolution via GraphQL (stable node IDs) + top-level PR comment for stale threads — cleanly separated active from outdated feedback
- Local binary testing end-to-end before marking PR ready (ran 18 checks, 6 findings, all addressed in one commit)

### What could've been better
- wk-pr-merge: should include reminder to manually update linked Jira ticket when MCP connection is unavailable (PR merge -> Done transition missing a fallback for MCP auth gaps)
- workflow: bash temp-file cleanup guards (trap EXIT INT TERM) should be standard in all shell instructions from first commit, not deferred to follow-up bot findings
