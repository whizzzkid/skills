## Session-1

### What worked
- Conflict resolution on a branch where the base PR had already merged: correctly identified the correct resolution by reading both sides and taking the more complete main-branch version for context-aware APIs.
- Adversarial review surfaced a real bug (3+ duplicate index tracking) that tests hadn't caught; fixed proactively before push.
- Bulk obvious-fix classification eliminated all per-item consultation for bot findings when all were Minor with no skip rationale.

### What could've been better
- pr-resolve: Ran `goimports -l` to check import formatting but did not run `goimports -w` to apply fixes — CI caught trailing-comment alignment on a new test file. Should run `-w` on every Go file written or edited, not just check with `-l`.
