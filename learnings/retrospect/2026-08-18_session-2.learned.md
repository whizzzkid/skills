## Session-2

### What worked
- Evaluating bot review comments against the actual call-site flow (session-action toggle) caught a real bug the bot only hinted at — always trace the runtime path, not just the local code.

### What could've been better
- [pr-update]: Generated-asset conflicts (store digest/screenshots) appeared 3 times during base merges. The skill should document a "regenerate from source" resolution pattern for generated-asset conflicts instead of side-picking, and should check whether the manifest's source-path set includes any file touched by the current commit to preempt stale-digest CI failures after committing non-asset changes.
