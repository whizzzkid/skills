## Session-3

### What worked
- De-bloat process: de-bloating every run (not just when prompted) successfully preserved all failure modes and rules while meeting size ceilings on 2 skills near their limits.
- Overfit scan: clean on all edits — no ticket/path/org tokens leaked.
- Staged only this batch's 21 paths, leaving 5 concurrent unprocessed learnings from other sessions unstaged; avoided bundling unrelated work.
- Hook fix (SIGPIPE in `check-readme-sync.sh`) now runs deterministically; install gate passed all pre-commit hooks without issue.

### What could've been better
- wk-retro: When self-catching an error or correction (e.g., discovering the SIGPIPE bug), invoke `wk-learn` immediately in the same response—before, alongside, or after the fix commit. Capturing at retro time loses precision about the exact error mode and the moment of discovery.
