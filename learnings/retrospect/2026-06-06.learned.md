
## Session-1

### What worked
- Adversarial review subagent caught the missing `-local` flag before push — the flag is non-obvious and would have caused CI/local import-grouping drift silently.
- Two-commit split (local dev tooling vs CI enforcement) kept the diff reviewable and each commit logically atomic.

### What could've been better
- [wk-self-review]: Pending review POST is blocked by auto-mode permission classifier; skill has no pre-flight step to warn the user or request permission before attempting the write.
- [wk-workflow]: When replacing a formatting tool, the planning phase should probe whether the replacement requires additional flags to behave identically — tool-flag parity is a planning checklist item, not a post-implementation adversarial catch.
