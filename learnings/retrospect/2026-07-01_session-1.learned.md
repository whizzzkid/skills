## Session-1

### What worked
- Disprove an automated reviewer's "new behavior" claim on extracted code by grepping the `-` side of the same diff for the identical construct before accepting the finding.
- Resolve a base-advance Gemfile.lock conflict by keeping the migrated single-remote structure and re-applying only the upstream version bumps, then verifying with a real dependency install.

### What could've been better
- pr-resolve: in Auto Mode, act on a confident recommendation for an already-decided finding instead of pausing to confirm it per-item.
