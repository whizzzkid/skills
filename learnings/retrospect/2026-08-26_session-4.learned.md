## Session-4

### What worked
- Gating a derived hazard-value on the same predicate that renders its consumer (not on whether inputs parse) is the structurally-correct fix; pin the invariant, never the regression.
- Replying to a bot thread via REST `/replies` (not GraphQL) correctly bypasses a self-authored pending/submitted review — reply published rather than being swallowed.

### What could've been better
- wk-workflow: do not call ScheduleWakeup to wait on a backgrounded task in a non-/loop session — the task-notification already re-invokes the agent on completion.
