## Session-4

### What worked
- Infra-vs-code CI failure classification (spot-instance exit 143) was immediate and correct — no wasted time diagnosing the diff
- Auto Mode disposition of all 6 bot findings (3 fix, 3 dismiss) completed without user intervention or pushback
- GraphQL `resolveReviewThread` for batch thread resolution was clean and reliable

### What could've been better
- wk-gh: GET-single-review-comment endpoint shape differs from POST-reply endpoint — skill lacks documentation distinguishing the two, causing six false 404s before root-causing the path shape
- wk-pr-resolve: no guidance on Sorbet `T.bind` requirement inside `prepend`-based mixin method bodies — two failed `srb tc` cycles before discovering `requires_ancestor` alone is insufficient
