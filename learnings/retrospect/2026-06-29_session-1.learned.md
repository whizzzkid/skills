## Session-1

### What worked
- SSRF URL-scheme guard applied and tested; pattern generalizes to any curl-fed API URL
- `with_retries` helper extracted cleanly with minimal friction; consolidated two identical retry loops

### What could've been better
- wk-pr-resolve: pending self-review blocks inline reply posting (422); skill should detect and surface pending reviews before attempting replies, not mid-flow
- wk-workflow: deferred a security validation without user instruction; "defer for follow-up" framing must not apply to guard clauses or security checks
- wk-pr-resolve: Buildkite pipeline step config location not probed before assuming `pipeline.rb`; skill should grep for the template/step key before editing
