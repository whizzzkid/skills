## Session-1

### What worked
- Adversarial subagent caught a sibling defect (same fix needed in a second function) before push, preventing a behavioral inconsistency from shipping.
- Outdated-thread (line: null) handling worked correctly — skipped REST reply, posted a top-level summary comment, resolved via GraphQL.

### What could've been better
- pr-resolve: When fixing a value/message reporting defect, the skill should instruct grepping the full file for all uses of that value before committing — sibling functions introduced by the same refactor often carry the same defect on a different line.
