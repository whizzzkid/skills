## Session-2

### What worked
- Adversarial review sweep 2.8 reliably catches PR body enumeration drift after a symbol rename/split — worth trusting as a blocker, not just a suggestion.
- Splitting a catch-all reason symbol into granular per-gate symbols surfaced cleanly; the 10-symbol refactor landed in one atomic commit and CI stayed green.

### What could've been better
- wk-pr-resolve: When presenting a judgment-required finding, prior-round dismissals on the same field are not surfaced. User had to re-state the same "keep for future use cases" rationale twice.
