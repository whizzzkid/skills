## Session-1

### What worked
- Centralizing divergent env-var fallback logic into three single-source-of-truth helpers (`reviewed_sha`, `reviewed_pr_number`, `reviewed_repo`) resolved all bot Major findings cleanly — the architectural move was right.

### What could've been better
- wk-pr-resolve: dismissed a bot Major finding about env-var divergence as "doesn't materialize" without verifying the forwarding contract documented in the codebase; the defect was real.
- wk-pr-resolve: argued against bot Major findings instead of reproducing them; user had to redirect explicitly before fixes were applied.
