## Session-2

### What worked

- Treat an explicit adversarial-review waiver as scoped authorization while preserving every other
  merge gate.
- Verify every pull request and the default branch against the atomic merge commit before cleanup.

### What could've been better

- wk-gh: Compare stack metadata with live pull-request head OIDs because stack metadata can lag
  commits pushed outside stack tracking; routed to `wk-learn gh`.
