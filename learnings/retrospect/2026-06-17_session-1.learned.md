## Session-1

### What worked
- Batch sharpen across consecutive runs: read each learning + its source retrospect, fold into the target SKILL.md as a mechanical sweep row or rule, write a principle reference, rename to .learned.md, then install/commit/push — clean and repeatable.

### What could've been better
- [sharpen]: Step 8 install must `cd` to the repo root explicitly — a prior `cd` into learnings/ during the rename step persisted (Bash cwd persists across calls), so the install ran in the wrong dir and printed "No valid skills found".
- [sharpen]: Run the prohibited-term/overfit scrub on archived learning and retrospect files before commit, not just on SKILL.md and reference edits — an internal tool name in an archive file blocked the commit at the hook.
