## Session-2

### What worked

- Drive the exact runtime harness that owns a compatibility stub before clearing a contract change.
- Keep the pending self-review anchored to the current head without submitting it.

### What could've been better

- [testing-skeleton]: Pair permission assertions with a strict fake that enforces the privileged API's invocation shape.
- [workflow]: Run the user-loadable development build after every gate that can overwrite its output directory.
- [adversarial-review]: Sweep production callers, unit fakes, injected globals, and browser harness stubs together
  after an API signature change.
