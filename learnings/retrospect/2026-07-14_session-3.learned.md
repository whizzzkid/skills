## Session-1

### What worked
- Real-time `wk-learn` capture at the moment each self-caught finding landed meant retro had nothing left to reconstruct — the consolidation pass stayed sparse, as intended.
- Returning a richer tuple `(value, flag)` from a mapping function so callers read the decision instead of re-deriving it eliminates a dual-encoding drift class at the source.

### What could've been better
- pr-resolve: the Step 11 retro tail-step was treated as opt-in under Auto Mode and skipped until the user asked why — an autonomous full-cycle run should either auto-invoke the retro or explicitly state it is deferring and why.
