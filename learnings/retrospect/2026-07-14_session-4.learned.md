## Session-4

### What worked
- Root-cause an order-dependent spec failure by reading the framework source and reproducing via deliberate reordering, not by trusting a single-file green run.
- Split a broad fail-closed rescue by error subtype so a should-never-happen config bug alerts while the expected steady state stays quiet.
- Dismiss a bot finding only with concrete evidence from the dependency's own source, never a bare assertion.

### What could've been better
- wk-pr-resolve: an author's PENDING self-review blocks both inline reply creation (422) and REST review-comment edits (404) — route around it via GraphQL resolveReviewThread plus a top-level conversation comment, and defer author-annotation edits until the pending review is submitted or dismissed.
