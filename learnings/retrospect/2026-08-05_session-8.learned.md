## Session-8

### What worked
- Preserve a concurrent pending review by rebuilding a deduplicated union before replacement.
- Validate interface contracts with focused temporary harnesses and source-backed architecture arithmetic without invoking optional live reviewers.

### What could've been better
- [wk-pr-review]: Treat model-backed local reviewers as explicit opt-in and consume only existing CI review output by default.
- [wk-gh]: Re-check the current user's pending review immediately before creating one because discovery state can become stale.
- [wk-workflow]: Invoke `wk-learn` in the same response as a correction or self-caught issue instead of batching capture at closeout.
