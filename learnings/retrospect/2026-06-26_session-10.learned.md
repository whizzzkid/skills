## Session-1

### What worked
- Refactoring 3 copy-paste artifact-URL methods into a shared helper + thin wrappers before adding a third artifact kept specs manageable and avoided triplication.
- Adversarial review sweep caught a stale code comment and missing bats test parity — both fixed before push.
- 3-commit structure (collect artifact → link in comment → add bats coverage) made each commit independently reviewable.

### What could've been better
- wk-workflow: When reformatting a UI-facing string (artifact links), audit existing spec assertion strings for the old literal before committing the change; spec negative-assertions matched stale text and required a second edit pass.
