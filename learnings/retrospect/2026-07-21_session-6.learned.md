## Session-6

### What worked
- Running the adversarial-review gate before every push caught a cross-file dead helper and a magic-number stride that both the human pass and the first bot round missed.
- Post-push, re-fetching review threads and looping fix→verify→push per new bot finding kept the PR converging instead of merging with open threads.

### What could've been better
- pr-resolve: a GitHub reply body was rendered with the commit-message footer variant instead of the canonical outbound footer, requiring a PATCH after posting — run the wk-gh footer pre-emit gate against the body BEFORE the POST, not after.
