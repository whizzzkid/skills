## Session-4

### What worked
- Adversarial review caught a real correctness bug (`defer renderer.Stop()` placement): first statement rule enforced, doc comment updated to match; fix was clean and targeted.
- `sync.Once` pattern for idempotent `Stop()` threaded correctly through signal handler and deferred call — no double-close panic risk.
- Non-blocking channel sends (`select { case ...: case <-r.quit: }`) prevented deadlock when sink events arrive after renderer is stopped.

### What could've been better
- wk-pr: PR created with wrong base branch on a stacked branch — Step 1 merge-base distance algorithm skipped; `--base` flag set to default branch without running the candidate sweep. (learning filed)
- wk-pr: PR body omitted the implementation plan document link — Step 2 body composition did not include a pre-flight to locate `docs/plans/` files covering the current phase. (learning filed)
