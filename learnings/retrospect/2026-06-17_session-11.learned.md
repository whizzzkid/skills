## Session-1

### What worked
- Three-surface fetch (inline threads + reviews + issue comments) + GraphQL resolve completed cleanly on first try with no retries.
- Adversarial subagent independently surfaced the nil-field serialization gap without being prompted — the subagent prompt design is working.
- Pending self-review submitted as COMMENT before replies — no HTTP 422 encountered.

### What could've been better
- [bk]: bk build view --json emits a Warning: prefix line to stdout when using env var auth, breaking direct jq pipe — requires tail -n +2 before jq in canonical pattern.
- [pr-resolve]: Bot issue comment carrying multiple distinct findings should be split into separate triage units, with a single combined reply covering all of them.
