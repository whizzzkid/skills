## Session-4

### What worked
- Fake binary pattern (tmpdir with fake `go`/`git` on PATH + `build_calls.log`) enables full integration testing of a cross-compile shell script without a real toolchain.
- Adversarial review caught two real defensive gaps (empty-string version comparison pass-through; stale artifact from prior run on reused agent) before marking the PR ready.

### What could've been better
- wk-commit: when `git push` is rejected non-fast-forward, the skill's push-rejection path should explicitly reference the global preference (merge, not rebase) so the agent defaults to `git pull --no-rebase` rather than rebasing and needing a force-push.
