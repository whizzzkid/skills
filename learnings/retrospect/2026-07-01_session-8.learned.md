## Session-8

### What worked
- Detecting false "ready" claims via `mergeStateStatus` and `reviewDecision` (source-of-truth API fields) instead of inferring from CI + thread count.
- Squash-merge workflow: auto-selecting best allowed method (squash → rebase → merge) on constraint.
