## Session-2

### What worked
- Compare a squashed parent with its original tip by tree before resolving conflicts, preserving validated content while
  recording the new ancestry.
- Validate the exact combined tree locally before preserving concurrent signed changes through a merge commit.

### What could've been better
- [wk-gh]: Unset a session-injected `GITHUB_TOKEN` when the user intends keyring-authenticated `gh`; routed and
  deduplicated through `wk-learn`.
- [wk-pr-merge]: Re-resolve pull request state before post-fix pushes and verify remote effects after interruptions;
  routed to `wk-learn`.
- [wk-learn]: Make interruption scanning runtime-aware and fall back to available conversation history; routed to
  `wk-learn`.
- [wk-markdown]: Let artifact-specific templates override generic heading requirements; routed to `wk-learn`.
