## Session-7

### What worked
- Test-mutation the guarded branch before trusting a regression test: remove the guard and confirm the new example fails, catching fixtures that satisfy the predicate without exercising the guarded path.

### What could've been better
- adversarial-review: enumerate each language-specific `#[]`/index semantics per receiver type when reviewing a type-guard test, since some non-target types (e.g. Ruby `String#[]`) silently return nil instead of raising — routed to wk-learn.
