## Session-7

### What worked
- Recovering a stacked child PR whose base branch was deleted on parent squash-merge by recreating the base ref, reopening, retargeting to trunk via REST, then `git rebase --onto` to drop squash-duplicated commits.
- Ruling out a nilable-association dereference finding by tracing a required `belongs_to` plus a `restrict_with_exception` parent instead of reflexively adding a nil guard.

### What could've been better
- testing-skeleton: request specs select a non-default response MIME with `as:`, never a `format:` keyword (which raises ArgumentError).
