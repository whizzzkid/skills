## Session-1

### What worked
- Verifying `rm -f ""` behavior in a playground before asserting safety — prevented a false claim.
- Fake-stub design gating body output on `--fail-with-body` presence makes plain `-f` regression detectable by the test suite.
- Correctly classifying an adversarial blocker as suggestion-only when the diff's intent was explicitly documented (test-branch gate removal).

### What could've been better
- [wk-buildkite]: Used `-w` flag to retrieve a build URL, which opened the browser instead.
- [wk-pr-resolve]: Skill started triaging before resolving merge conflicts; user had to provide an explicit reminder.
