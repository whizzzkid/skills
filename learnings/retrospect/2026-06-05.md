## Session-1

### What worked
- Adversarial subagent caught missing YAML anchor (`&name`) + pre-push wiring on a new lefthook hook before push; blocker resolved in one cycle.
- Security hook flagged shell command injection pattern (`$(gofmt -l {files})`) in the original hook command; switching to autofix (`gofmt -w`) eliminated the risk.

### What could've been better
- wk-adversarial-review: When writing a new lefthook command tagged `pre-commit-and-pre-push`, the initial draft omitted the YAML anchor and pre-push wiring — the adversarial agent caught it, but the initial implementation should have probed sibling commands for the anchor pattern first.

## Session-2

### What worked
- Adversarial review caught a cross-doc enumeration sync gap (mandatory check list count stale) before the PR was created — saved a review cycle
- wk-learn was invoked in real time when the user called out the issues (not deferred to retro)

### What could've been better
- wk-pr: PR body behavioral claims were narrated from intent rather than derived by re-reading the implementation — routed to wk-learn
- wk-workflow: wk-retro was skipped after `gh pr ready`; marking ready felt like a natural terminus but the retro is part of the workflow contract — routed to wk-learn
- wk-pr-resolve: narrow user directive ("fix description, mark for review") caused Step 11 (wk-retro) to be silently dropped — routed to wk-learn

→ Skills routed to wk-learn: wk-pr, wk-workflow, wk-pr-resolve
