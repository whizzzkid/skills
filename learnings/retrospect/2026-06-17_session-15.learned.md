## Session-15

### What worked
- Adversarial subagent surfaced a return-value gate divergence (helper returns bool; caller re-checked the original precondition) and an empty-blob edge case, both before push — catching real clobber paths the mechanical sweeps missed.
- Defaulting to "apply all" for suggestions-only verdict in auto mode kept the session tight: two small fixes (3 lines each), one spec addition, pushed in a single follow-up commit.

### What could've been better
- adversarial-review: sweep catalog lacks an explicit check for callers that re-test the triggering precondition (`X.success?`) instead of capturing a helper's return value — the gap was caught by the LLM pass, not a mechanical sweep.
