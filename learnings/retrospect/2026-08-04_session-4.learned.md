## Session-4

### What worked
- Preserve the browser's numeric package version while using descriptive manifest metadata for human-facing
  development labels.
- Inspect platform-generated actual, expected, and diff images before accepting only the intended visual baselines.

### What could've been better
- [wk-adversarial-review]: Flag build-time branch discovery that invokes git before provider branch metadata or permits
  detached `HEAD`.
- [wk-workflow]: Include a CI-artifact review step whenever a visible change affects platform-specific visual baselines.
