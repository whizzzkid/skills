## Session-5

### What worked

- Re-run failed jobs on the same commit to separate deterministic regressions from transient failures.
- Exercise test fixtures under both pull-request and default-branch contexts when environment-derived values reach rendered output.

### What could've been better

- wk-gh: Pass an explicit pull request to `gh pr view --web` when `--repo` is present; routed to `wk-learn`.
