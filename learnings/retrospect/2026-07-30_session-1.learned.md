## Session-1

### What worked

- Split a user-visible behavior change into three independently green stacked PRs and verified exact
  commit trees before publication.
- Paired real-browser happy and restricted paths with a deliberate failure-artifact proof before
  declaring the stack ready.

### What could've been better

- workflow: Run dependent verification commands fail-fast so an expected-red proof cannot trigger
  later suites and cascade into orphaned browser processes; routed to `wk-learn workflow`.
