
## Session-1

### What worked
- Adversarial subagent correctly identified MarshalJSON-without-UnmarshalJSON as a write-only trap and the missing compound-format serialization test — both were fixed before push without a reviewer round.
- Mechanical sweep 2.10 caught PR body drift on `bareCheckName`/`CheckName` references before push, closing the stale-doc loop in the same session.

### What could've been better
- wk-pr-resolve: triage ALL comments before implementing any fix — user had to interrupt mid-implementation of Comment 2 because Comments 3 and 4 had not been presented yet.

## Session-2

### What worked
- Pre-merge gate (CI + threads + reviews) ran atomically and all-clean; no manual verification needed.
- Detected "already merged" state gracefully via explicit `state == MERGED` check before declaring success.

### What could've been better
- wk-pr-merge: `gh pr merge` fails in git worktree environments when the base branch is checked out in the parent worktree; add `--repo $GITHUB_ORG/{repo}` to the merge command as the worktree-safe invocation form.
## Session-3

### What could've been better
- wk-pr-resolve: when all active bot findings carry Minor severity, the skill enters the full per-item consultation loop before offering a bulk-dismiss path. The all-Minor case should present a single gate ("bulk dismiss all Minor findings?") before triage begins, saving multiple consultation rounds.
