## Session-4

### What worked

- Verify the installed extension's command set against its official source before choosing a
  fallback, then use the atomic stack operation and confirm every member's remote state.

### What could've been better

- wk-pr: Prescribe stack-aware cascading rebase, conflict continuation, remote-head verification,
  and description sync for an existing pull request stack; routed to `wk-learn pr`.
- wk-pr-merge: Detect stack membership before the single-pull-request path and prefer the atomic
  stack merge after validating every included pull request; routed to `wk-learn pr-merge`.
