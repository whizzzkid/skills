## Session-1

### What worked
- Adversarial review caught the critical findings-path parsing bug (wrong output line) before first push — the two-commit approach (feat then fix) kept the diff reviewable
- R2/R7 resilience design (warn-and-continue when binary exists, scope gitignore advice to repo root) correctly modeled the boundary conditions without over-engineering

### What could've been better
- [wk-workflow]: When parsing CLI output in skill instructions, verify stdout vs stderr and flag-conditioned vs always-emitted lines before writing grep commands — a wrong line assumption silently produces empty results
- [wk-workflow]: Claude Code plugin cache takes priority over worktree installs; add a step to copy updated SKILL.md to cache path or start a fresh session when developing plugin skills
