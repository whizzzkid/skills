## Session-5

### What worked
- Piping a fresh adversarial subagent narrated intent (not just the raw diff) sharpened its verification focus on a narrowly-scoped merge/bugfix review.
- Routing bot-thread replies around a pending self-review via a top-level comment plus GraphQL thread resolution avoided the 422 that blocks inline REST replies.

### What could've been better
- pr-merge: account for a host permission-classifier denial of `gh pr merge` as a failure mode distinct from branch protection — explain the two-layer model and treat a manual merge as the already-MERGED path rather than retrying.
