## Session-1

### What worked
- Using the repo's own authoritative validator (`--check`) plus the conformance suite as the review gate for a data-only CSV change kept the adversarial pass fast and grounded.
- Refreshing the row comments ("level-50 gate" → "level-47 gate") in the same commit as the value change avoided leaving stale narration.

### What could've been better
- learn: scan-mode project-slug transform collapses only `/` and `_`, so a dotted username breaks the transcript-dir match; broaden to any non-alphanumeric.
