## Session-4

### What worked
- Deriving a published URL from an existing config env var (single source of truth) with a validated https+host parse and a hard fallback avoided a second config knob.
- Splitting combined rejection-scenario specs into one example each kept later failures from being masked by the first.
- Adversarial review caught two publish-surface URL risks (hostless https parse, userinfo leaking into a published link) that mechanical sweeps and unit tests missed.

### What could've been better
- self-review: staging a new pending review 422s when a stale pending review exists — the create path should detect and delete the prior draft (preserving bodies) before POSTing.
